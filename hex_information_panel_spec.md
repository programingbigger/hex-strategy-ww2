# 機能仕様書：ヘックス情報パネル

## 1. 概要
プレイヤーがマップ上のヘックスにカーソルを合わせた際、そのヘックスに関する「地形情報」と、そこに存在する「ユニット情報」を画面隅の固定パネルに統合して表示する。これにより、プレイヤーは戦術判断に必要な情報へ常に容易にアクセスできる。

## 2. UI/UX仕様
- **配置:** 画面のいずれかの隅（例：右下）に情報を表示するパネルを常設する。
- **表示タイミング:** カーソルが新しいヘックスに移動した直後、遅延なくパネル内の情報を更新する。

## 3. 表示コンテンツ
情報パネルは、主に「地形情報」と「ユニット情報」の2つのセクションで構成される。

### 3.1. 地形情報
カーソルが置かれているヘックスの地形に関する情報を表示する。

- **地形スウォッチ:**
  - 地形を象徴する色や簡易アイコンを表示し、視覚的な識別を助ける。
- **地形名:**
  - 「平地」「森林」など、地形の正式名称をテキストで表示する。
- **基本効果:**
  - 防御ボーナスや視界への影響など、ユニットの種類を問わない固定の効果をリスト表示する。

### 3.2. ユニット情報
ヘックス上にユニットが存在する場合、そのユニットの基本情報を表示する。（既存の`HOVERED UNIT`情報をこのパネルに統合）

### 3.3. 動的情報（インタラクティブ要素）
静的な情報に加え、プレイヤーのアクションに応じて詳細情報を引き出せるインタラクティブな要素を設ける。

- **ユニット別移動コスト（プルダウン形式）:**
  - 「**▼ユニット別移動コスト**」のようなタイトルで、初期状態では折りたたまれた項目を設ける。
  - プレイヤーがこの項目をクリック（またはタップ）すると、項目が展開される。
  - 展開されたリストには、主要なユニットタイプ（例：歩兵、車両、航空機など）ごとに対象地形を移動する際の消費コストが一覧で表示される。
  - これにより、UIの煩雑さを避けつつ、必要に応じて詳細な戦術情報を提供できる。

## 4. データ管理
本機能で表示するデータ、特に複雑になりがちな移動コストについては、保守性と一貫性を高めるために以下の方針で管理する。

### 4.1. 移動コストデータの一元化
- **方針:**
  - 地形ごと、およびユニットタイプごとの移動コストは、すべて単一のJSONファイル（例: `data/movement_costs.json`）で定義する。
  - このJSONファイルが、ゲーム内における移動コストの唯一のマスターデータ（Single Source of Truth）となる。
- **参照箇所:**
  - 既存のゲームロジック（`constants.ts`等で定義されているもの）
  - 本機能「ヘックス情報パネル」のプルダウン表示
- **メリット:**
  - バランス調整等で移動コストを変更する際、JSONファイルを修正するだけでゲーム全体に反映される。
  - データ定義がコードから分離されるため、可読性とメンテナンス性が向上する。
- **JSON構造（例）:**
  ```json
  {
    "forest": {
      "infantry": 1,
      "vehicle": 2,
      "heavy_vehicle": 3
    },
    "mountain": {
      "infantry": 2,
      "vehicle": null,
      "heavy_vehicle": null
    },
    "plains": {
      "infantry": 1,
      "vehicle": 1,
      "heavy_vehicle": 1
    }
  }
  ```